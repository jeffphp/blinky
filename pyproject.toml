[project]
name = "blinky-mcp"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "hidapi>=0.14",
    "mcp[cli]>=1.26.0",
    "pyserial>=3.5",
]
